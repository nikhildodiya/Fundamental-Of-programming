//Find circumference of square formula


#include<stdio.h>

int main() // Main Function
{
    int area, a; // Variable For Store The Value

    // User's Input    
    printf("Enter length: ");
    scanf("%d", &a);

    area = 4 * a; // Formula For Find Area Of Cube

    // Takes User's Output
    printf("circumference of Square: %d", area);
}